{
  "name": "@teleporthq/react-libraries",
  "version": "0.0.0",
  "description": "A experimental setup for running the libraries in the playground",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "npm run lint -- --fix",
    "validate": "ts-node scripts/validate.ts",
    "dev": "ts-node scripts/watcher.ts",
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@mojotech/json-type-validation": "^3.1.0",
    "@teleporthq/teleport-types": "^0.12.4",
    "@teleporthq/teleport-uidl-validator": "^0.12.4",
    "@types/jest": "^26.0.10",
    "@types/node": "^14.6.0",
    "@typescript-eslint/eslint-plugin": "^3.9.1",
    "@typescript-eslint/parser": "^3.9.1",
    "chokidar": "^3.4.2",
    "eslint": "^7.7.0",
    "eslint-config-prettier": "^6.11.0",
    "jest": "^26.4.0",
    "prettier": "^2.0.5",
    "pretty-quick": "^2.0.1",
    "ts-jest": "^26.2.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged && npm run lint"
    }
  }
}
